using System.ComponentModel;

namespace Model.Enums
{
    public enum TemplateSearchAreaTypesEnum
    {
        SELECT_EVERY_MONITOR,
        SELECT_MONITOR,
        SELECT_APPLICATION_WINDOW,
        SELECT_CUSTOM_AREA
    }
}
