<Page
    x:Class="StepinFlow.Views.Pages.Executions.TemplateSearchLoopExecutionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:System="clr-namespace:System;assembly=mscorlib"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:enum="clr-namespace:Model.Enums;assembly=Model"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:StepinFlow.Views.Pages.Executions"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="TemplateSearchLoopExecutionPage"
    d:DataContext="{d:DesignInstance local:TemplateSearchLoopExecutionPage,
    IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Page.Resources>
        <ObjectDataProvider
            x:Key="FlowStepTypesEnum"
            MethodName="GetValues"
            ObjectType="{x:Type System:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="enum:FlowStepTypesEnum" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Page.Resources>

    <Grid VerticalAlignment="Top">


        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>




        <StackPanel Grid.Column="1">

            <Label>Name</Label>
            <TextBox
                Margin="0,0,0,20"
                IsReadOnly="True"
                Text="{Binding ViewModel.Execution.FlowStep.Name, Mode=TwoWay}" />

            <Label>Process</Label>
            <TextBox
                Margin="0,0,0,20"
                IsReadOnly="True"
                Text="{Binding ViewModel.Execution.FlowStep.ProcessName}" />

            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <StackPanel>
                    <Label>Set accuracy</Label>
                    <TextBox
                        Margin="0,0,0,20"
                        IsReadOnly="True"
                        Text="{Binding ViewModel.Execution.FlowStep.Accuracy}" />
                </StackPanel>
                <StackPanel>
                    <Label>Result accuracy</Label>
                    <TextBox
                        Margin="0,0,0,20"
                        IsReadOnly="True"
                        Text="{Binding ViewModel.Execution.ResultAccuracy}" />
                </StackPanel>
            </StackPanel>


            <Label>Template image.</Label>
            <Image
                MaxWidth="100"
                MaxHeight="100"
                Source="{Binding ViewModel.Execution.FlowStep.TemplateImage}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="ImageFailed">
                        <i:InvokeCommandAction Command="{Binding ViewModel.ImageFailedCommand}" />
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseDown">
                        <i:InvokeCommandAction Command="{Binding ViewModel.TemplateImageDoubleClickCommand}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Image>


            <Label>Result image</Label>
            <Image Source="{Binding ViewModel.ResultImage}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="ImageFailed">
                        <i:InvokeCommandAction Command="{Binding ViewModel.ImageFailedCommand}" />
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseDown">
                        <i:InvokeCommandAction Command="{Binding ViewModel.ResultImageDoubleClickCommand}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Image>



        </StackPanel>
    </Grid>
</Page>